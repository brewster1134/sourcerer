spec:
  sourcerer:
    errors:
      sourcerer:
        install:
          multiple_packages_found: multiple_packages_found %{package_name} %{package_types}
          no_package_found: no_package_found %{package_name}
      package:
        search:
          any_type:
            no_packages_found: no_packages_found %{package_name} %{version}
          single_type:
            no_package_found: no_package_found %{package_name} %{version} %{type}
    cli:
      install:
        installing_package: installing_package %{package_name} %{type} %{destination}
        multiple_packages_found: multiple_packages_found %{package_name}
        no_package_found: no_package_found %{package_name}
