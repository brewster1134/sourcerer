spec:
  sourcerer:
    errors:
      install:
        multiple_packages_found: multiple_packages_found %{package} %{types}

    cli:
      install:
        installing_package: installing_package %{package} %{type} %{destination}
        multiple_packages_found: multiple_packages_found %{package}
