spec:
  sourcerer:
    errors:
      cli:
        install:
          no_package_found: no_package_found %{name}
      sourcerer:
        install:
          multiple_packages_found: multiple_packages_found %{name} %{version} %{types}
          no_package_found: no_package_found %{name}
      package:
        download:
          failed: failed %{name} %{version}
        method_not_defined: method_not_defined %{method_name} %{type}
        search:
          failed: failed %{name} %{type}
        version:
          failed: failed %{name} %{version}
    package:
      download_success: download_success %{name} %{version} %{destination}

    cli:
      install:
        description: description
        multiple_packages_found: multiple_packages_found %{name}
        pending: pending %{name} %{version} %{type}
        success: success
        options:
          destination:
            description: description
          force:
            description: description
          type:
            description: description
          version:
            description: description
      help:
        description: description
